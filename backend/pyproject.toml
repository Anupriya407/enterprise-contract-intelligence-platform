[project]
name = "ecip-backend"
version = "0.1.0"
description = "Enterprise Contract Intelligence Platform Backend"
requires-python = ">=3.13"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"